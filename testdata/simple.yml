name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker://alpine:3.18
      - uses: ./local/action@v1
      - uses: ../local/action@v1
      - name: foo
        uses: /abs/local/action@v1
      - uses: https://example.com/action@v1
      - width:
          a: b
        uses: actions/setup-go@v4 # comment
      - uses: caarlos0/meta/.github/workflows/govulncheck.yml@v0.1.0 # comment lalal
      - uses: docker/setup-qemu-action@v2 # comment
      - uses: docker/setup-buildx-action@v2
